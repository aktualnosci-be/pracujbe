# Faza 2 — issues i poprawki (wspólne zasady dla wszystkich agentów)

Repo GitHub: aktualnosci-be/pracujbe. Narzędzia GitHub: mcp__github__* (załaduj przez ToolSearch, np.
"select:mcp__github__issue_write,mcp__github__create_pull_request,mcp__github__add_issue_comment,mcp__github__pull_request_read").
Nie ma `gh` CLI. Git push działa normalnie (`git push -u origin <branch>`).

## Twój worktree
Masz WŁASNY worktree `/workspace/wt/<nazwa>` (node_modules to symlink — nie uruchamiaj `npm install/ci`).
Nie dotykaj /workspace/pracujbe ani cudzych worktree. Dla KAŻDEJ poprawki (osobny PR) utwórz świeżą gałąź z aktualnego main:
  git -C /workspace/wt/<nazwa> fetch origin main && git -C /workspace/wt/<nazwa> checkout -b claude/public-<krótki-temat> origin/main
Jeśli masz niescalony wcześniejszy PR, a nowa poprawka zależy od niego — NIE nakładaj; wybierz niezależną albo zakończ i zgłoś.

## Własność plików (twarda reguła)
Edytujesz WYŁĄCZNIE pliki ze swojej listy właścicielskiej (poniżej w poleceniu). Gdy poprawka wymaga cudzego pliku — nie
edytuj, opisz to w raporcie końcowym. Poza wszystkimi: src/messages/*.json mają BLOKADĘ (niżej), docs/design/people-passport/README.md
i CLAUDE.md edytuje tylko integrator (nie ruszaj). Nie zmieniaj gałęzi/PR innych sesji (#159, #178, #179, #183, #187, #198, #199, #128, #133, #70, #74)
i ich obszarów (panele candidate/employer/admin, wiadomości, e-mail, metadata/generateMetadata/OG/tytuły SEO w page.tsx, CI workflow).

## Blokada tłumaczeń src/messages/*.json
Przed edycją JSON-ów: `mkdir /tmp/claude-0/-workspace-pracujbe/27ac77b3-7ae4-53c9-8e96-07974b43b582/scratchpad/locks/messages`
(atomowe; jeśli istnieje → blokada zajęta: wybierz poprawkę bez nowych kluczy albo zakończ i zgłoś). Po uzyskaniu wpisz
do `locks/messages/owner` swoją nazwę i nr PR. NIE usuwaj blokady sam — zwalnia ją integrator po scaleniu. Dodawaj klucze we
wszystkich 4 językach (pl/nl/fr/en), bez RO/UK. Test i18n-keys musi przejść.

## Budowanie i testy
- Build: `flock /tmp/claude-0/-workspace-pracujbe/27ac77b3-7ae4-53c9-8e96-07974b43b582/scratchpad/locks/build.lock npm run build` (w worktree; max jeden build naraz).
- E2E: `WT=/workspace/wt/<nazwa> PORT=<twój port> npx playwright test --config /workspace/wt/pw.config.mjs <plik lub grep>`
  (uruchamiaj z katalogu worktree; serwer = next start z twojego .next; po zmianach kodu przebuduj).
  Zabij swój serwer po testach (`pkill -f "next start -p <port>"`). Serwer audytowy :3100 zostaw.
- Przed pushem: `npm run lint && npm run typecheck && npm test` + właściwe E2E (nowe i dotknięte) — wszystko zielone.
- Test regresyjny do każdego bugfixa, testujący ZACHOWANIE (nie powtarzający implementacji). Kontrola ujemna OBOWIĄZKOWA:
  tymczasowo przywróć błąd (git stash / odwróć zmianę), pokaż że test pada, przywróć poprawkę. Opisz to w PR.
- Stringi UI tylko z i18n. Invariants z CLAUDE.md.

## Issues
Twórz issues TYLKO z przydzielonej listy (deduplikacja zrobiona przez integratora). Przed utworzeniem szukaj duplikatu
(mcp__github__search_issues). Format (po polsku), tytuł z prefiksem `P1:`/`P2:`; labels: `bug` (+ `P1` dla P1):
## Problem / ## Kroki odtworzenia (URL, viewport, język) / ## Obecny wynik / ## Oczekiwany wynik / ## Zakres (ekrany, języki)
/ ## Kryteria odbioru (checkboxy, w tym test regresyjny z kontrolą ujemną) / ## Priorytet (uzasadnienie). Treść na
podstawie odtworzenia (twój raport w audit/). Każdy post na GitHubie (issue, komentarz, PR) kończ stopką:

---
_Generated by [Claude Code](https://claude.ai/code)_

## PR
Jeden PR = jedna logiczna poprawka, mały diff. Przed pushem sprawdź slot CI: `mkdir .../scratchpad/locks/ci-slot-1`
(albo -2, -3; pierwszy wolny). Bez wolnego slotu NIE pushuj — zostaw gałąź lokalnie gotową i zgłoś w raporcie.
Wpisz nazwę gałęzi do `locks/ci-slot-N/branch`. Jeden zwalidowany push; bez pustych commitów i „odświeżających” pushy.
Commit message po polsku, zakończony liniami:

Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01XVJ4Hs1HCb3iGP8yRhBPkV

PR do `main` (brak szablonu PR w repo). Treść: ## Zmiana, ## Dlaczego (Closes #N), ## Testy (co uruchomiono lokalnie +
kontrola ujemna), ## Ryzyko, ## Cofnięcie. Na końcu treści PR:

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XVJ4Hs1HCb3iGP8yRhBPkV

NIE scalaj PR-ów (robi to integrator po zielonym CI). Nie uruchamiaj ponownie jobów CI.

## Raport końcowy (krótki)
Utworzone issues (nr), PR-y (nr, gałąź, head SHA), wyniki testów i kontroli ujemnej, zajęte blokady, gałęzie gotowe lokalnie
bez pusha, potrzebne zmiany w cudzych plikach.
