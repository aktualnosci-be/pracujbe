## Zmiana
- `FilterSidebar` jest kolumną flex o ograniczonej wysokości. Pola filtrów przewijają się we własnym obszarze (`data-filter-scroll="desktop"`), a przycisk „Pokaż N ofert” i ewentualny alert licznika zostają pod nimi, zawsze widoczne.
- Lista ofert (`oferty-pracy/page.tsx`) przekazuje panelowi `max-h-[calc(100dvh-7rem-var(--cookie-banner-h,0px))]`, czyli wysokość viewportu minus przyklejony nagłówek i ewentualny baner cookies. Zmienną wystawia PR agenta cookies. Fallback `0px` działa przed jego scaleniem.
- Nowy test E2E `tests/e2e/jobs-list-sidebar-reach.spec.ts`.

## Dlaczego
Closes #216. Panel miał ~1911 px wysokości i był `sticky` bez własnego przewijania. Przy 1280×800 dolne sekcje i przycisk zatwierdzenia pojawiały się dopiero przy ~4400 px z 4896 px, czyli pod ostatnią kartą oferty. Filtr wyglądał na niedziałający, bo zmiany czekają na kliknięcie „Pokaż”.

## Testy
- `npm run lint`, `npm run typecheck`: zielone.
- `npm test`: 610/611. Jedyny błąd to `tests/unit/organic-story-assets.test.ts`, który pada też na `main` w tym środowisku, bo brakuje przeglądarki `chromium_headless_shell-1228` dla skryptu generującego PNG. Nie ma związku z tą zmianą.
- E2E (Chromium, `next start` z tego builda): `jobs-list-*`, `job-filter-*`, `a11y`, `public-zoom`, `smoke`, `flows` dają 55/56. Jedyny błąd to `jobs-list-header.spec.ts` (no-JS, `documentElement` null po nawigacji formularzem). Ten test jest niestabilny też na niezmienionym `main` (2 na 32 powtórzenia na serwerze z `main`).
- Nowy test `jobs-list-sidebar-reach.spec.ts` (1280×800 i 1024×700): w połowie listy wyników przycisk zatwierdzenia jest w viewporcie, sekcja „Data dodania” daje się przewinąć wewnątrz panelu bez przewijania strony do stopki, a zatwierdzenie zmienia URL.
- **Kontrola ujemna:** po cofnięciu zmian w `src/` (osobny build) oba przypadki padają na `toBeInViewport` przycisku zatwierdzenia (linia 62). Z poprawką oba przechodzą.
- Sprawdzone też scalenie próbne z pozostałymi gałęziami tego obszaru (#220, #224, #228, #230, #233): brak konfliktów, typecheck i E2E zielone.

## Ryzyko
Niskie. Zmiana dotyczy tylko układu panelu na ≥ lg. Mobilny `FilterSheet` i formularz no-JS są bez zmian. Pierścienie fokusu w obszarze przewijania mają zapas 4 px (`-mx-1 px-1 pt-1`).

## Cofnięcie
`git revert` tego commita.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01XVJ4Hs1HCb3iGP8yRhBPkV
